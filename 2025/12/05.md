 ##  取り組んだ課題一覧

- 【JS】ガチで学びたい人のためのJavaScriptメカニズム (【修了確認】クラスと継承 ~ 反復可能オブジェクト（イテレーター）)

 ##  わかったこと

- `setTimeout(bob.hello(bob), 1000);`のように記述すると、この関数がオブジェクトに紐づいていない状態で実施される。<br>bob.hello()というbobオブジェクトを参照したいのに、グローバル変数を参照してしまう。<br>結果、オブジェクトは出力されず、helloのみ出力され`this.name`(こんかいはbob)は出力されない

- チェーンメソッドにしたい時は、メソッドの{}の中に、`return this`と記述する

- `for...in`列挙可能プロパティに対して順不同で反復処理を実行する

- `for...of`イテレーターを持つオブジェクトの反復操作を行う

- `イテレーター`反復操作を行う際に使用するオブジェクト<br>`String` `Array` `Map` `Set` `arguments`など

 ##  次やること

- 【JS】ガチで学びたい人のためのJavaScriptメカニズム

 ##  感じたこと

- イテレーターという新しい用語が出てきて困惑したけど、すでに使っているオブジェクトだった。このように名前は知ってるけど、すでに使っているものは他にもあると思うので、新しい用語と自分の知っている知識を繋げて理解に努める事が大切だと感じた

 ##  学習時間
- today: 3h
- month: 9.25h
- total: 404h
