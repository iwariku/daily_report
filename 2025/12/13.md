 ##  取り組んだ課題一覧

- JavaScript Primer - 迷わないための入門書(コマンドライン引数を処理する ~ Todoアプリのリファクタリング) 修了 
- モダンJavaScriptの基礎から始める挫折しないためのReact入門(Propsを知る ~ カイゼン(TODOの上限設定)) 修了
- Reactの公式チュートリアルをやる

 ##  わかったこと

- コンポーネントとは、独自のロジックと外見を持つUI(ユーザーインターフェース)の部品のこと

- `Props`とは、コンポーネントに渡す引数。

- コンポーネントに引数を渡す時の記述<br>`<ColorfulMessage color="blue">`
- `<ColorfulMessage color="blue">お元気ですか？</ColorfulMessage>` のタグで囲われた部分(お元気ですか？)を受け取るのは、`{props.children}`で受け取る事ができる。

- `State`とは、それぞれのコンポーネントが持っている状態を、管理するもの。

- `import { useState } from "react";`を宣言<br>`cosnt[状態を保持する変数名, 状態を更新させる関数名] = useState();`

- `useState()`などのHook(use〇〇)は、関数コンポーネントの一番上の階層でしか呼ぶ事ができない。

- `useEffect()`とは、どの変数に関心をもって、その変数の変更を判定していくかを見るHook。

- React(JSX)でCSS用のクラスをあてたい時は、`<div className="クラス名">`とする。

- `map`や`filter`なども一緒で、配列の値を基に一覧表示するという実装の時に、配列をループしてreturnしている一番上の要素に`key`を設定しなければならない。Reactの裏側で動いている仮想DOMが何個目の要素なのか？という点を見つけるために一意の値を入れないといけない。
    ```
    return (
        <li key={todo // この部分}>
            <div className="list-row">
                <p className="todo-item">{todo}</p>
                <button>完了</button>
            </div>
        </li>
    );
    ```
 
 ##  次やること

- Reactの公式チュートリアルをやる

 ##  感じたこと

- コンポーネントとは？React Hookとは何か？理解が深まってよかった。
- ReactでTodoListを作成したことで、どのようにHookが使われているかが理解できた。
- 前にReactを学習した時は、今よりもJavaScriptの基礎を固めていない時で、Reactの理解を深まりにくかったが、JavaScriptの基礎を固めて、学習すると理解の深まり方が全然違った。Reactの次はNext.jsがあるので、Reactの基礎固めをしっかり行う！

 ##  学習時間
- today: 10h
- month: 45.75h
- total: 440.5h
