 ##  取り組んだ課題一覧

- Golangでtwitter cloneを作る(フォロー機能のDB関連)
- TypeScript完全攻略(15. オプション引数とデフォルトパラメータ ~ 18. 演習問題)

 ##  わかったこと

- 相互のデータを考える時は、主となるデータが何なのか？目的のデータは何なのかという点を明確にすると、データ構造が見えてくる。

- `INNER JOIN`を使うことで、データの整合性をとることができる。<br>(例)フォロー一覧の際に、ユーザーの情報を紐付ける。`follows`テーブルにusersテーブルを結合する。`INNER JOIN`を使うことで、usersテーブルにない(退会したユーザー)は、フォロー一覧に表示されない。

### TypeScirpt
- `never型`は関数が最後まで到達しない。決して戻ってこない。

##  次やること

- Golangでtwitter cloneを作る(フォロー機能)
- TypeScript完全攻略(19. ~)

 ##  感じたこと

- 分からないかったことを朝に取り組んだら、理解できた。学習において朝がとても重要なので、高負荷なアウトプットはなるべく朝に取り組むようにするのがいいと思った。

 ##  学習時間
- today: 7.5h
- month: 64.75h
- total: 974.75h