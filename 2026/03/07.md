 ##  取り組んだ課題一覧

- Golangでtwitter cloneを作る(ブックマーク(リファクタリング)のPR提出)
- TypeScript完全攻略(7. 型注釈と3大プリミティブ ~ 14. アロー関数の型定義)

 ##  わかったこと
### Go
- 引数で空の`interface{}`は使わない。型の恩恵が受けられない。

- 無理して関数を作ってコードが冗長になるより、メソッド内で、処理を行う場合が良いこともある。
    ```
 	// ツイートレスポンスにマッピングする時の処理
    // 理由はsqlcでは関数名Row型ができてしまうので、結局1回しか使わない専用関数ができてしまうだけだから
    tweetRes := make([]TweetResponse, len(dbTweet))
	for i, t := range dbTweet {
		tweetRes[i] = TweetResponse{
			ID:           t.ID,
			UserID:       t.UserID,
			Content:      t.Content,
			LikeCount:    t.LikeCount,
			IsLiked:      t.IsLiked,
			RetweetCount: t.RetweetCount,
			IsRetweeted:  t.IsRetweeted,
			IsBookmarked: t.IsBookmarked,
		}
	}
    ```


### TypeScript
- `unknown型`を使うことでで、型チェックがされるまでは中身のデータに参照できない、安全装置として機能することができる
```
// 型ガードを使った安全な利用フロー

```

- `undefined`・・・「未定義」、まだ値がない
- `null`・・・「空」、意図的に何もない

- `retryCount: number = 3`のように変数宣言時に = デフォルト値とすることで、デフォルト値を設定できる

##  次やること

- Golangでtwitter cloneを作る(フォロー機能)
- TypeScript完全攻略(14. アロー関数の型定義 ~)

 ##  感じたこと

- リファクタリングが難しいと感じた。動くコードは書けるが、設計に準じているか？再利用性は？逆に関数に定義する必要のないものもあった。コードの品質という点において学びが多かった。

 ##  学習時間
- today: 8h
- month: 57.25h
- total: 967.25h