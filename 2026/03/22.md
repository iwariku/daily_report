##  取り組んだ課題一覧

- ReactにTypescriptを導入する(ソート機能、コンポーネント分割)

##  わかったこと
```tsx
  const students = allInstances.filter(
    (user): user is Student => user instanceof Student,
  );
```
- `user is Student`とすることで、型を`Student`に固定していいよということをTypeSctiptに伝えている。

- `sort(a, b)`というソートロジックは、2つづつ比べて並べ変える。<br>`a - b`は「昇順」で、`b - a` は「降順」

##  次やること

- TypeScriptクイズをやる(10 ~)
- ReactにTypescriptを導入する

 ##  感じたこと

- コンポーネントを作成する時には、stateとロジックの部分を分けて考えないと、どちらが原因でエラーが発生しているのかがわからなくなるため、都度、状況を整理することが大切だと感じた。

 ##  学習時間
- today: 8h
- month: 165h
- total: 1075h

