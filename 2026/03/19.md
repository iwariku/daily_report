 ##  取り組んだ課題一覧

- Golangでtwitter cloneを作る(DM機能PR修正、退会機能のPR提出)
- React with TypeScript

##  わかったこと

- 状態管理のことを`state`といい、いいねボタンのカウント数などの状態の時に使う。
- `state`は状態を示す変数。
- `setState`は状態を更新する関数。
- `useState`の()にはstateに入れる初期値を記述する
    ```
    cosnt [state, setState] = useState(1)
    ```

- `props`とは、コンポーネントに値を渡す仕組みのこと

##  次やること

- サバイバルTypeScript(非同期処理 ~ )
- TypeScriptクイズをやる(10 ~)
- React with TypeScript

 ##  感じたこと

- バックエンドに関してであるが、最後の方はAI無しで実装できた。またAIに質問する際も自分の中で仮説を立ててから質問できるようになったので、理解力が前と全然違うという点も感じれてよかった。

 ##  学習時間
- today: 8h
- month: 142h
- total: 1052h