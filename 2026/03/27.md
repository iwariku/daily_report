##  取り組んだ課題一覧

- ReactにTypescriptを導入する(PR修正)
- TypeScriptクイズをやる(11 ~ 20)
- Next.js公式チュートリアル(9章 ~ 11章)

##  わかったこと

- `s.matchedMentor = [matchedMentorNames];`のように、`state`で扱っている要素(同じ参照)を直接書き換えるのは、あまり良くない。<br>元々のオブジェクトを展開し、新しくインスタンス作成するようにすれば、元のオブジェクトを上書きせずにデータを返却することができる。
    ```
    const students = rawStudents.map((s) => {
    const matchedMentorNames = s.getMatchedMentorNames(rawMentors);

    // 2-1 インスタンスのプロパティ(matchedMentor)に直接代入
    // ...s というインスタンスのコピーしてオブジェクトを作成すると、メソッドが消えてしまう
    // StudentDataでmatchedMentorはstring[]と定義されているので、配列にして入れます

    // 2-2. stateを直接変更するのは「Stateは不変に扱う」とう原則に反するため、以下の文は使わない。
    // s.matchedMentor = [matchedMentorNames];

    // 2-3 1と2の理由により、新しいインスタンスを作成するやり方にする
    // s を展開しつつ、対応するメンターを上書きしたオブジェクトを渡す
    const newStudent = new Student({
      ...s,
      matchedMentor: [matchedMentorNames],
    });

    // 3. 新しく作成したインスタンスを返す（これで型が Student のまま維持される）
    return newStudent;
  });
    ```

- コンポーネント直下は、「純粋」である必要があり、`Date.now()`や`Math.random()`のような、毎回値が変わるものは書かない。

- `React`の`props`について、状況にもよるが、5階層`props`を渡す様であれば設計を見直す必要がある。

##  次やること

- TypeScriptクイズをやる(20 ~ )
- Next.js公式チュートリアル(12章 ~ )

 ##  感じたこと

- Next.jsを学べば学ぶほどNext.jsの凄さを感じる。DB操作があったときに、SQLを書きながら挙動のイメージができて、バックエンドを学んでおいてよかった。やっぱり自分はDBを操作したりするバックエンドの方が好きなのかもしれないと感じた。

 ##  学習時間
- today: 10h
- month: 205h
- total: 1113h
