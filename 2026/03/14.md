 ##  取り組んだ課題一覧

- Golangでtwitter cloneを作る(フォローPR修正)

##  わかったこと

 - サブクエリはレコード(行)の分だけ実行されるので、大量のデータの取得には向いていない。


| 取得データ数 | 推奨される手法 | 推奨される手法 |
| ---- | ---- | ---- |
| 1件 | サブクエリ | 実行回数が1回で済むため。<br>クエリの構造がシンプルに保てる |
| 大量 | JOIN句 | 行数分のループ処理を避け、セット(集合)として一括処理できるため。 |

##  次やること

- Golangでtwitter cloneを作る(DM機能)
- TypeScript完全攻略(50. keyof と typeof 演算子 ~)

 ##  感じたこと

- SQLの文法よりも、実際にデータを扱う場面を考えるとSQLの理解がより深まると感じた。

 ##  学習時間
- today: 3h
- month: 108.75h
- total: 1018.75h