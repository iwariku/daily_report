##  取り組んだ課題一覧

- twitter_GoのフロントにNext.jsを導入する(メッセージ機能PR提出)

##  わかったこと

- Next.jsの`Form`コンポーネントは、内部で「**内部で渡された関数(action)に対して、`formData`という引数を1つだけ入れて実行する**」という動きをする。<br>なので、`      <Form action={() => createMessage(formData, id)}>`のように記述すると、引数は1つです。というエラーが発生する。

##  次やること

- twitter_GoのフロントにNext.jsを導入する。5/01までに

 ##  感じたこと

- フロントエンドにジェネリクスを入れてみた。ユニオン型 → ジェネリクスとしたので、学習したばかりは苦手意識があったけど、今回の実装で使えてよかった。

 ##  学習時間
- today: 8h
- month: 153h
- total: 1298h