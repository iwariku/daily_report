##  取り組んだ課題一覧

- twitter_GoのフロントにNext.jsを導入する(フォロー機能)

##  わかったこと

- `const { follow_list: followingList } = await getFollowings(id);`とすることで、分割代入した変数名を変えることができる`{APIから返却されるキー名 : 新しく宣言する変数名}`

- フロントエンドでの「トランザクション的な振る舞い」は、エラーが起きたら`catch`ブロックで`useState`で元に戻す。

- `revalidatePath()`は特定のパスに対して

##  次やること

- twitter_GoのフロントにNext.jsを導入する。4/19までに

 ##  感じたこと

- トランザクションやロールバックのことを考えて実装すると、エラー時のデータの整合性が損なわれるということを感じた。また、AIに一度コードレビューもらうとコードの全体をみてもらうことができるので、活用していこうとおもった。

 ##  学習時間
- today: 5.5h
- month: 100.75h
- total: 1245.75h
