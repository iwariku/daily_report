##  取り組んだ課題一覧

- 【この動画1本でOK】Next.js 完全攻略【2025年最新 Version15】 
- Googleカレンダークローン(前月・翌月のボタンで表示を切り替えPR提出)

##  わかったこと

- `CSR`とは、サーバーから最小限のHTMLとJavaScriptをクライアントに送り、クライアント側でHTMLを生成する手法。
- `SSR`とは、サーバー上でReactのコードが実行されてHTMLを生成し、クライアントに送る手法。

- `ISR`とは、buildする際にHTMLを生成し、特定の時間が経ったら際buildされる。

- `SSG`とは、アプリをbuildする際にHTMLを生成し、リクエストされた際にHTMLを返す手法。

##  次やること

- TypeScriptクイズをやる(20 ~ )
- Googleカレンダークローン(週、月ボタンで表示を切り替え)

 ##  感じたこと

- アウトプットを通してNext.jsの理解が深まっている。ただ、今はReactの拡張的な部分までしかできていないので、Next.jsのApp Routerの部分をもっと理解できるように頑張る。

 ##  学習時間
- today: 8h
- month: 16h
- total: 1161h