 ##  取り組んだ課題一覧

- Golangでtwitter cloneを作る(サインアップAPI完成・フロント完成 PR提出、ユーザーアクティベーション)

 ##  わかったこと

- `c.JSON()`でhttpレスポンスを決めることができる。これを記述することで、postmanにレスポンスを表示できる。(cはgin.Context型)

- `c.JSON()`だけではエラーがlogに表示されないので、`log.Print()`メソッドも併用して、postman(JSON形式で表示)とターミナル表示する必要がある。

- postgresのvolumesは「初期化SQLをコンテナに渡す」

- ginのvolumesは「ローカルのソースコードをコンテナから参照する」イメージはリアルタイム同期

- ユーザーアクティベーション(有効化)とは「そのメールアドレスが本当に本人のもので、使える状態にあるか」を最終確認すること

##  次やること

- Golangでtwitter cloneを作る(フロント作成)

 ##  感じたこと

- sqlcやginで生成されたコードを理解しておかないと、後々わからなくなる。自分で書いていないコードだからこそ、AIと対話して理解する必要があると感じた。

 ##  学習時間
- today: 8h
- month: 24h
- total: 758h
