 ##  取り組んだ課題一覧

- Golangでtwitter cloneを作る(ユーザーアクティベーションPR修正、ログイン機能作成)

 ##  わかったこと

- インターフェースを挟むことで、本番を渡すことやテストを渡すなど自由に変更がきく。DI(依存性注入)の考え方

- `session`とは、ユーザーごとの一時的な状態(ログイン状態など)を覚えておく仕組み

- `redis`とは、超高速なインメモリ型のデータの保存場所

- `docker-compose.yaml`の`depends_on`はコンテナの起動順序をコントロールできる指令等。これがないと、すべてのコンテナが同時に動き出してしまい、使いたいコンテナが使えない場合がでてくる。

##  次やること

- Golangでtwitter cloneを作る(ログイン機能)

 ##  感じたこと

- どれだけリファクタリングしても関数や変数が増えてしまうだけなので、構造体やインターフェース、DIを使って実装をしていく必要があると感じた。

 ##  学習時間
- today: 8h
- month: 41h
- total: 775h
