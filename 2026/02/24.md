 ##  取り組んだ課題一覧

- Golangでtwitter cloneを作る(いいね機能)

 ##  わかったこと

- DB設計では「行為」や「イベント」は関係そのもの
  - 多対多→テーブルでは直接表現できないので、中間テーブルを作る。その中間テーブルとなるのがlikesテーブル

- `sqlc`は`SELECT *`を使わずにカラムをしていした場合、「このクエリ専用の型」として`Row型`を生成することがある。`Row型`が生成されたことで、型違いというエラーが起きることがある。

##  次やること

- Golangでtwitter cloneを作る(いいね機能)

 ##  感じたこと

- データ構造を先に決めておくのめっちゃ大切。データ設計をやっているかやっていないかで、実装のやりやすさが段違いと感じた。

 ##  学習時間
- today: 8h
- month: 151h
- total: 885h
